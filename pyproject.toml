[project]
name = "carnatic-music-transcription"
version = "0.1.0"
description = "Transcribe Carnatic music svara notation to Western sheet music via LilyPond"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
transcribe = "transcribe_song:main"
